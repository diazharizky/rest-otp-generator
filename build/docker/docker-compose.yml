version: "3.4"
services:
  redis:
    image: redis:6.0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 3s
      retries: 30
    ports:
      - 6379:6379
    volumes:
      - redis:/data

volumes:
  redis:
